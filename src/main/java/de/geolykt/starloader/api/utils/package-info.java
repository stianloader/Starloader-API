/**
 * General-purpose classes used throught the API.
 * Sometimes however they are not used within the API for one reason or another,
 * this does however not mean that you shouldn't use them. As long as they are not deprecated
 * classes within this package should be treated as usual API.
 */
package de.geolykt.starloader.api.utils;
